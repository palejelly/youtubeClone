extends layout.pug 

block customContent 
    form(method="GET").search__form
        input(placeholder = "Seearch by title", type="text",name="keyword")
        button 
            i.fas.fa-search


    div.video-grid 
        each video in videos 
            +video(video)
        else             
            span.empty__message No videos found
